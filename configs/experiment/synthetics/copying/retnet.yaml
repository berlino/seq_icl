# @package _global_
defaults:
  - /experiment/synthetics/copying/base.yaml
  - /model/layer: retention

model:
  _name_: lm_simple
  d_model: 32
  n_layer: 2
  attn_cfg:
    n_heads: 8
  d_inner: ${eval:4 * ${.d_model}}
  vocab_size: ${dataset.vocab_size}
  resid_dropout: 0.0
  embed_dropout: 0.1

